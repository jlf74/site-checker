node_modules/
.next/
out/
build/
coverage/

.env
.env.local
.env*.local

npm-debug.log*
.DS_Store

# локальная база почт (MVP) — никогда не коммитить
data/

# локальные настройки и рабочие копии Claude Code
.claude/settings.local.json
.claude/worktrees/


# сырые прогоны моделей — разбор чужих сайтов, публиковать нельзя
benchmarks/*
!benchmarks/README.md
