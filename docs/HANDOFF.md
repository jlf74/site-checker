# Где мы сейчас

> Состояние на 13 августа 2026. Документ для передачи контекста в новую сессию.
> Долгосрочные решения — в [SPEC.md](SPEC.md), план — в [ROADMAP.md](ROADMAP.md).
> Этот файл описывает незавершённую работу; когда она закончится — его можно удалить.

## Главное: проверки на боевом сервере не работают

**Anthropic блокирует запросы из России.** Приложение развёрнуто в Москве, и вызовы
к Claude возвращают:

```
403 {"error":{"type":"forbidden","message":"Request not allowed"}}
```

Это географическая блокировка, а не проблема ключа или кода. Локально всё работает
только потому, что у Юлии включён VPN с выходом через Литву — проверено, компьютер
виден интернету как литовский.

Получается структурный конфликт двух обязательных требований:

- **152-ФЗ** требует хранить данные россиян в РФ — поэтому хостинг в Москве
- **Anthropic** не обслуживает запросы из РФ

Пока это не решено, домен подключать бессмысленно и запускаться нельзя.

## Что решаем прямо сейчас

Сравниваем качество российских моделей с Claude. Если российская модель справляется —
это устойчивый путь: ничего не обходим, никто не отключит аккаунт, оплата в рублях.
Если проваливается — возвращаемся к варианту с ретранслятором за границей, понимая риск.

**Обе российские модели проверены 13.08.2026 и не подошли.** Подробности и цифры —
в [SPEC.md](SPEC.md#технологии-и-хостинг), сырые прогоны в `benchmarks/runs/`.
Один лендинг, одни промпты, один и тот же текст страницы:

| Модель | Критичных (юрблок) | Оценка сайта | Проверка 152-ФЗ | Время | Цена |
|---|---|---|---|---|---|
| **Opus 5** (эталон) | 3 | 5/100 | 5 находок, включая cookie-баннер | 185 с | 47 ₽ |
| YandexGPT Pro | 1 | 76/100 | **0 находок**, cookie-баннер засчитан как «в порядке» | 46 с | ? |
| GigaChat-3-Ultra | 1 | 48/100 | 1 находка, cookie-баннер пропущен | 43 с | ? |

Обе российские модели проходят мимо нарушений 152-ФЗ — а это ядро продукта. Покупатель
получил бы противоположный вердикт по своему сайту. Дело не в промптах: промпты одни и те же.

**Значит, российского пути нет, и решение сузилось до одного вопроса — как дотянуться
до Claude из РФ.** Ретранслятор в Амстердаме работает наверняка, но обход географического
ограничения может противоречить условиям Anthropic — рискуем аккаунтом, на котором весь
продукт. Это решение за Юлией; технически ретранслятор ещё не проектировался.

### Варианты, которые рассматривали

| Вариант | Суть | Риск |
|---|---|---|
| **Российская модель** | YandexGPT или GigaChat вместо Claude | Качество может просесть — это и проверяем |
| Ретранслятор за границей | Маленькое приложение в Амстердаме (Timeweb даёт такой регион), через него идут вызовы Claude. Москва остаётся для сайта и базы, 152-ФЗ соблюдён | Обход географического ограничения может противоречить условиям Anthropic — аккаунт могут закрыть, а на нём весь продукт |
| Реселлер Claude в РФ | Те же вызовы чужими руками | Тот же риск + наценка + трафик через третье лицо |

VLESS-туннель не подходит: на Timeweb App Platform запускается только приложение,
поставить рядом системный клиент некуда. Cloudflare Workers тоже не годятся —
их сеть может направить запрос через российскую точку и блокировка повторится.

### Готовность к сравнению

- **YandexGPT — прогнан 13.08.2026, не подошёл** (см. выше). Доступ работает: `yandexgpt` (Pro,
  версия 09.02.2025) и `yandexgpt-lite` (25.03.2025). Ключ и folder id в `.env.local`.
  Обёртка — `benchmarks/yandex.mjs`, проверка доступа — `benchmarks/check-yandex.mjs`.
  `yandexgpt-lite` не прогоняли: Pro уже провалился, lite слабее.
- **GigaChat — доступ настроен и прогнан 13.08.2026, не подошёл** (см. выше).
  Сертификаты Минцифры (корневой + промежуточный) скачаны с gu-st.ru в
  `benchmarks/russian_trusted_ca.pem` — **только файл в папке проекта**, в системное
  хранилище Windows ничего не ставилось. Подключается на одну команду:
  `NODE_EXTRA_CA_CERTS="D:/AI Проекты/site-checker/benchmarks/russian_trusted_ca.pem"`.
  Откат — удалить файл. Отключать проверку сертификатов нельзя, и не понадобилось.
  Доступны модели `GigaChat-2`, `-2-Pro`, `-2-Max`, `GigaChat-3-Ultra` (прогоняли Ultra).
  Обёртка `benchmarks/gigachat.mjs` понимает и пару client_id+secret, и единый Authorization key.

### Как устроено сравнение

**Тестовая страница:** https://veronika-hatskevich.ru/health-and-nutrition
(курс нутрициологии на Tilda — есть формы, обещания результата, медицинские
утверждения, продажа обучения. Задевает все три юридические проверки.)

Прогонщик — `benchmarks/run.mjs`, инструкция в `benchmarks/README.md`. Он берёт боевые
промпты и боевой разбор ответа, а текст страницы качает один раз и кладёт в
`benchmarks/pages/` — все модели получают одинаковый вход. Новую модель добавлять туда же.

Результаты прогонов класть **только** в `benchmarks/` (папка не уходит в git):
прошлый эталон лежал в `C:\Temp`, она очистилась, и прогон пришлось делать заново.

**Эталон Opus 5 от 13.08.2026** (effort=low, все шесть проверок): 8 критичных находок,
риск 6,32 млн ₽, оценка 5/100, 185 секунд, 47 ₽ за полный отчёт. Только юрблок —
3 критичных, риск тот же. Прошлая запись «4 критичных, 4,76 млн» относилась к другому
состоянию промптов; сравнивать модели надо с этим прогоном.

Из четырёх контрольных находок Opus нашёл три: cookie-баннер без кнопки отказа,
дискредитацию конкурентов («другие специалисты бессильны»), отсутствие цены и условий.
Четвёртую — сбор специальных категорий ПД в переписке о здоровье — в этот раз
сформулировал шире: «не видно уведомления о передаче данных третьим лицам (мессенджеры)».
Если решим усилить, это правка промпта `152fz`, а не смена модели.

## Что уже сделано и работает

**Приложение развёрнуто:** https://jlf74-site-checker-a090.twc1.net
Серверная часть живая, ссылки «режим разработчика» на проде нет. Сама проверка
не работает — из-за блокировки Anthropic выше.

⚠️ **На 14.08.2026, 17:00 оба адреса отдают 502 — приложение не отвечает.**
Проверено шесть раз за две минуты, не разовый сбой. Домен тут ни при чём: технический
адрес лежит так же. Смотреть в панели Timeweb «Логи приложения» и статус на вкладке
«Деплой»; проверить, не поставлено ли приложение на паузу (кнопка «II» в шапке).

**Домен `sait-checkup.ru` только куплен — и всё.** Reg.ru, зарегистрирован 13.08.2026,
оплачен до 13.08.2027, NS `ns1/ns2.reg.ru`. **К приложению его не подключали**, сайт
на нём не поднимали, сертификат не выпускали, `www` не настраивали.

Когда дойдут руки, порядок такой: добавить домен в панели Timeweb → прописать
выданные записи в DNS Reg.ru → дождаться расхождения → включить SSL (Let's Encrypt) →
поменять `NEXT_PUBLIC_SITE_URL` на `https://sait-checkup.ru` и **пересобрать**
(от неё зависит адрес og-картинки, сейчас там технический адрес Timeweb).

Замечание, которое стоит проверить в панели: на 14.08.2026 `sait-checkup.ru` и `www`
резолвились на `37.252.23.182` — тот же IP, что и технический адрес приложения.
Само по себе это ничего не подключает; при настройке убедиться, что существующие
записи не конфликтуют с тем, что выдаст Timeweb.

Остальные три домена из плана по whois .RU на 13.08.2026 были **свободны**:
`saitcheckup.ru`, `dok-checkup.ru`, `сайт-чекап.рф`. Проверить статус можно в любой момент —
whois-запрос к `whois.tcinet.ru`, порт 43.

**Сделано в коде за 12–13 августа:**

- Починен баг, из-за которого проверки молча падали: `max_tokens: 4000` был общим
  потолком на размышления модели и на ответ, JSON обрывался. Теперь 16000
- Упавшая проверка больше не притворяется успешной — раньше она давала пустой
  результат, и оценка сайта завышалась
- Промпты откалиброваны на живых ответах: критичных находок было 9, стало 4;
  суммарный риск с 7,97 млн упал до 4,76 млн; убраны обрубленные суммы штрафов
  и дубли одной находки в трёх проверках
- Добавлена настройка глубины размышлений `CHECK_EFFORT` (по умолчанию `low`):
  проверка на Opus 5 подешевела с 12,7 до 5,2 ₽ и ускорилась с 81 до 30 секунд
- Результат бесплатной проверки живёт час в браузере (localStorage), на сервер
  не пишется. Час не продлевается при повторном заходе
- Написана публичная оферта — страница `/offer`, ссылки в футере и у кнопки оплаты

**Сделано в коде 14 августа:**

- Системные окна браузера (`alert`) заменены на нормальные состояния — у «Войти»
  и у кнопки оплаты. В коде `alert()` не осталось
- Доступность: вторичный текст доведён до контраста WCAG AA (`--c-faint` затемнён
  с `#8595AC` до `#5F6E8A`), добавлен видимый фокус с клавиатуры (`:focus-visible`)
- Шрифтовая пара: **Onest** — заголовки, вердикт, заголовки находок и кнопки;
  **Manrope** — весь остальной текст. Обе с кириллицей, через `next/font`
- og-картинка генерируется кодом при сборке (`app/opengraph-image.js`), 1200×630,
  шрифты для неё лежат в `assets/fonts/`. Отдельного макета держать не нужно
- Превью настоящего отчёта на главной; данные примера вынесены в `lib/example-report.js`
  и общие со страницей `/report-example`
- Страница 404 с маскотом вместо стандартной страницы Next
- Типографический акцент на деньгах: сумма риска 34px (`components/RiskLine.js`),
  штраф в находке 20px над статьёй закона
- Маскот получил настроение `ready` — улыбается шире, пока курсор в поле адреса

**Оферта — решение от 14.08.2026:** идём с текущей редакцией, без вычитки юристом.
⚠️ Помнить: оферта описывает состояние после этапа 2 — в ней обещаны личный кабинет,
выгрузка PDF и бесплатная перепроверка. Ничего из этого пока не построено, поэтому
включать приём оплаты можно только вместе с ними.

## Грабли, на которые уже наступили

- **Timeweb:** Next.js лежит во вкладке Frontend (в Backend его нет). Тумблер
  «Поддержка SSR» **обязателен** — без него собирается статика и все `/api/`
  перестают существовать. Доступен только в Москве и Амстердаме. Директория сборки
  по умолчанию подставляется `/out` — это для статики, нужно `.next`
- **`NEXT_PUBLIC_DEV_UNLOCK` на проде не задавать.** Иначе на сайте появляется
  ссылка, открывающая платный отчёт даром. Переменные `NEXT_PUBLIC_*` вшиваются
  при сборке — поменять после деплоя недостаточно, нужна пересборка
- **`CHECKUP_SECRET` обязателен в проде**, иначе роуты намеренно падают
- Не запускать `npm run build` при работающем dev-сервере — делят папку `.next`
- Кириллический домен `.рф` не годится основным: почта на нём принимается не всеми
  сервисами, а вход в кабинет у нас по ссылке на почту. Только редиректом
- Документацию Яндекса нельзя читать автоматически — отдаёт капчу

## Где что лежит

- `.env.local` — все ключи (Claude, GigaChat, Yandex). В git не уходит
- `benchmarks/` — прогоны моделей и обёртки к API. В git не уходит намеренно:
  там разбор чужих сайтов с юридическими претензиями, а репозиторий публичный
- `docs/SPEC.md` — продуктовые решения и их обоснования
- `docs/ROADMAP.md` — план по этапам
