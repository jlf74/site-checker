// Каталог проверок СайтЧекап.
// Два блока: юридическая проверка (главный товар) и тексты (бонус).
// Каждая проверка — отдельный запрос к Claude с требованием строгого JSON-ответа.

const JSON_FORMAT = `
Формат ответа — ТОЛЬКО валидный JSON без markdown-обёртки и пояснений:
{
  "summary": "вывод по проверке одной-двумя фразами, человеческим языком",
  "findings": [
    {
      "severity": "critical" | "warning" | "ok",
      "title": "суть находки: что именно не так, конкретно и без вводных слов (до 90 знаков)",
      "quote": "точная цитата с сайта, к которой относится находка, или null",
      "law": "статья закона, например «152-ФЗ, ст. 9», или null если не про закон",
      "fine": "вилка штрафа для ИП/юрлиц по КоАП РФ, например «до 300 000 ₽», или null если находка не про нарушение закона",
      "fineMax": число — верхняя граница штрафа в рублях (для суммы риска), или null,
      "fix": "конкретное исправление: что и как изменить, 1-3 предложения, или null для severity ok"
    }
  ]
}
Правила:
- severity "critical" — прямое нарушение закона или ошибка, которая явно вредит (риск штрафа, потеря заявок);
- severity "warning" — рискованная формулировка, недочёт, который стоит поправить;
- severity "ok" — то, что проверено и в порядке (обязательно включай такие пункты, 2-5 штук);
- максимум 12 находок, сначала critical, потом warning, потом ok;
- "fine" и "fineMax" указывай только по КоАП РФ для первичного нарушения (не повторного), не завышай;
- заголовок — суть проблемы, а не констатация процесса («В формах нет согласия на обработку данных», а не «Была проведена проверка форм»);
- "fix" — готовое действие в повелительном наклонении («Добавьте…», «Замените на…»), а не «рекомендуется рассмотреть»;
- пиши по-человечески, без канцелярита;
- если текста мало для уверенного вывода — скажи об этом в summary и не выдумывай находки.`;

export const CHECKS = [
  {
    id: '152fz',
    label: '152-ФЗ',
    fullLabel: 'Персональные данные (152-ФЗ)',
    group: 'legal',
    defaultOn: true,
    prompt: `Ты — практикующий юрист по защите персональных данных в РФ (152-ФЗ). Проверь текст сайта:
1. Формы сбора данных (телефон, почта, имя): есть ли рядом согласие на обработку персональных данных.
2. Упоминается ли политика конфиденциальности и ссылка на неё.
3. Назван ли оператор персональных данных (ИП/ООО с реквизитами).
4. Понятны ли цели обработки данных.
5. Есть ли уведомление об использовании cookies (если по тексту видно, что сайт собирает аналитику).
Штрафы бери из ст. 13.11 КоАП РФ (актуальные вилки для юрлиц, первичное нарушение).
Учитывай: ты видишь только текст страницы, без HTML. Если элемент может существовать, но не виден в тексте, — severity "warning" с оговоркой, а не "critical".${JSON_FORMAT}`,
  },
  {
    id: 'advertising',
    label: 'Реклама',
    fullLabel: 'Закон о рекламе (38-ФЗ)',
    group: 'legal',
    defaultOn: true,
    prompt: `Ты — практикующий юрист по рекламному праву РФ (38-ФЗ «О рекламе»). Проверь текст сайта:
1. Превосходные степени без документального подтверждения: «лучший», «№1», «самый», «единственный».
2. Гарантии результата там, где результат гарантировать нельзя (доход, похудение, излечение, «100%»).
3. Некорректные сравнения с конкурентами.
4. «Бесплатно» со скрытыми условиями.
5. Медицинские и финансовые заявления без обязательных оговорок.
Для каждого нарушения указывай статью 38-ФЗ в "law" и вилку штрафа по ст. 14.3 КоАП РФ для юрлиц в "fine"/"fineMax".${JSON_FORMAT}`,
  },
  {
    id: 'education',
    label: 'Образование',
    fullLabel: 'Закон об образовании (273-ФЗ)',
    group: 'legal',
    defaultOn: true,
    prompt: `Ты — юрист по образовательному праву РФ (273-ФЗ «Об образовании»). Проверь текст сайта:
1. Продаются ли курсы/обучение/тренинги — и есть ли лицензия на образовательную деятельность либо корректная оговорка об информационно-консультационных услугах.
2. Названа ли организация-исполнитель.
3. Есть ли обещания «диплома», «сертификата государственного образца» без оснований.
4. Корректно ли описаны программы.
Штрафы: ст. 19.20 КоАП РФ (деятельность без лицензии) и смежные.
Если по тексту сайт не продаёт обучение — одна находка severity "ok" с пояснением, что проверка неприменима, без выдуманных проблем.${JSON_FORMAT}`,
  },
  {
    id: 'spelling',
    label: 'Опечатки',
    fullLabel: 'Опечатки и ошибки',
    group: 'text',
    defaultOn: false,
    prompt: `Ты — профессиональный редактор русского языка. Найди в тексте сайта:
1. Орфографические ошибки и опечатки.
2. Пунктуационные ошибки.
3. Грубые стилистические ошибки (канцелярит, тавтологии, несогласованность).
Ошибки и опечатки — severity "warning" (это не штраф, но подрывает доверие покупателя). Грубые ошибки в заголовках и на видных местах — "critical".
Поля "law", "fine", "fineMax" всегда null. В "quote" — точный фрагмент с ошибкой, в "fix" — как правильно.${JSON_FORMAT}`,
  },
  {
    id: 'ux',
    label: 'Продающие тексты',
    fullLabel: 'Убедительность текстов',
    group: 'text',
    defaultOn: false,
    prompt: `Ты — маркетолог и специалист по конверсии. Оцени тексты сайта глазами покупателя:
1. Заголовок: понятна ли выгода за 5 секунд.
2. Описания услуг: конкретика или общие слова.
3. Призывы к действию: мотивируют ли, понятно ли, что произойдёт после клика.
4. Доверие: цифры, кейсы, отзывы, гарантии.
5. Отработаны ли типовые возражения и страхи клиента.
Поля "law", "fine", "fineMax" всегда null. В "fix" давай готовую переформулировку, а не совет «сделать лучше».${JSON_FORMAT}`,
  },
  {
    id: 'seo',
    label: 'SEO',
    fullLabel: 'SEO-базис',
    group: 'text',
    defaultOn: false,
    prompt: `Ты — SEO-специалист. Оцени текст страницы:
1. Ясно ли из текста, что предлагает сайт и для кого (тематическая релевантность).
2. Структура: есть ли логика заголовков и разделов, или это «простыня».
3. Достаточно ли текста для индексации, нет ли дублирующихся кусков.
4. Есть ли призывы к действию.
Поля "law", "fine", "fineMax" всегда null.
Учитывай: ты видишь только текст, без метатегов и разметки — не делай выводов о title/description/H1.${JSON_FORMAT}`,
  },
];

export const CHECK_GROUPS = [
  { id: 'legal', label: 'Юридическая проверка' },
  { id: 'text', label: 'Бонусом: тексты' },
];

export function getCheck(id) {
  return CHECKS.find((c) => c.id === id) || null;
}

// Оценка сайта 0–100 по совокупности находок всех выполненных проверок.
export function computeScore(allFindings) {
  let score = 100;
  for (const f of allFindings) {
    if (f.severity === 'critical') score -= 12;
    else if (f.severity === 'warning') score -= 4;
  }
  return Math.max(5, Math.min(98, score));
}

// Суммарный риск штрафов в рублях по найденным нарушениям.
export function computeRisk(allFindings) {
  return allFindings
    .filter((f) => f.severity !== 'ok' && typeof f.fineMax === 'number' && f.fineMax > 0)
    .reduce((sum, f) => sum + f.fineMax, 0);
}

export function verdictForScore(score, criticalCount) {
  if (criticalCount > 0 && score < 50) return 'У сайта серьёзные проблемы — риск штрафов реальный';
  if (criticalCount > 0) return 'Сайт неплох, но есть риски, которые лучше убрать';
  if (score >= 85) return 'Отличный результат — серьёзных проблем не нашли';
  return 'Хороший сайт, но есть что поправить';
}
